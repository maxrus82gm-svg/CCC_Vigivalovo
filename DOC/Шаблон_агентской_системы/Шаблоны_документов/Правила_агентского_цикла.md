---
проект: "{{PROJECT_NAME}}"
тип: правило
раздел: агентская система
статус: шаблон
источник: универсальный шаблон 0.4
---

# Правила агентского цикла

Родительская ветвь: `Карта_агентской_документации`.

## Назначение

Канонический владелец ролей, режимов, заморозки опубликованной TASK, жизненного цикла и REVIEW Ассистента.

Запуск TASK определяет [[Правила_запуска_задач]], Git-политику — [[Правила_Git_и_проверки]], локальную работу — одноимённый канонический документ, формат результата — [[Отчёт_агента]]. Полные правила здесь не дублируются.

## Роли

### Пользователь

- принимает конечные и архитектурные решения;
- управляет Git и опасными операциями;
- публикует большую TASK и отправляет малый запуск либо передаёт самостоятельную небольшую задачу;
- передаёт отчёт Агента Ассистенту и подтверждает новый цикл.

### Ассистент ChatGPT

- выбирает между большим циклом и самостоятельной небольшой задачей;
- подписывает тип блока снаружи copy-paste области;
- для большого цикла создаёт компактную большую TASK и отдельный обязательный малый запуск;
- до отчёта передаёт изменения только малыми уточнениями;
- после отчёта сначала проверяет доступное актуальное удалённое состояние репозитория, затем выполняет REVIEW;
- формирует следующую большую TASK только после решения по предыдущему циклу.

### Локальный Агент

- в большом цикле читает активную TASK и канонические правила, а в самостоятельной небольшой задаче следует полученному малому блоку;
- выполняет только разрешённый scope;
- применяет уточнение только к затронутой части;
- записывает отчёт и предварительный статус;
- не объявляет результат принятым.

### Субагенты

Работают только внутри делегированного scope. Основной Агент перепроверяет результат и отвечает перед Пользователем.

## Режимы

- `Ограниченный` — реализация и границы заданы; неоднозначная часть останавливается.
- `Самостоятельный` — Агент выбирает безопасный способ внутри заданной цели, архитектуры и scope.

Если режим не указан, применяется самостоятельный режим без права расширять scope или постоянные правила.

## Замороженная TASK

После публикации и до итогового отчёта БЛОК 1 заморожен. TASK нельзя переписывать, заменять или редактировать из-за уточнений.

Уточнения передаются отдельными малыми сообщениями. Позднее явное уточнение имеет приоритет только для затронутой части. Неразрешимый конфликт останавливает только спорную часть.

## Жизненный цикл

Ассистент сначала выбирает форму по [[Правила_запуска_задач]].

### Большой цикл

1. Ассистент создаёт большую TASK и малый запуск.
2. Пользователь публикует TASK в БЛОКЕ 1 и отправляет малый запуск Агенту.
3. Агент выполняет работу; уточнения приходят только малыми сообщениями.
4. Агент обновляет БЛОКИ 2–3 и при полном выполнении очищает содержимое БЛОКА 1.
5. Пользователь передаёт отчёт Ассистенту и самостоятельно управляет Git.
6. Ассистент выполняет REVIEW и рекомендует закрытие, уточнение ещё активной частичной TASK, новую TASK или откат Пользователем.

### Самостоятельная небольшая задача

1. Ассистент выдаёт подписанный снаружи `МАЛЫЙ БЛОК — НЕБОЛЬШАЯ ЗАДАЧА`.
2. Пользователь отправляет copy-paste блок прямо в чат Агента.
3. Агент выполняет короткую работу и возвращает краткий отчёт в чат.
4. БЛОКИ 1–3 не изменяются без прямого указания.
5. Пользователь передаёт отчёт Ассистенту для REVIEW.

Отчёт завершает локальный этап, но не означает принятие результата. После отчёта по текущему проекту Ассистент сначала проверяет доступный удалённый репозиторий по [[Правила_Git_и_проверки]] и явно показывает статус commit. Завершённая TASK задним числом не переписывается. После очистки БЛОКА 1 любое следующее изменение является новой задачей, форму которой Ассистент выбирает по масштабу.

## Постоянные правила

Постоянные правила меняются только по отдельной TASK, основанной на решении Пользователя.
